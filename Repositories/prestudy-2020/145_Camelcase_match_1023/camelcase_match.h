#ifndef CAMELCASE_MATCH_H
#define CAMELCASE_MATCH_H
#include <vector>
#include <string>

class Solution {
public:
    std::vector<bool> camelMatch(std::vector<std::string>& queries, std::string pattern);
};

#endif